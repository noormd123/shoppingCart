# shoppingCart
1. Type "npm install" to install all the node packages
2. Run the Build by "npm run build"
3. Serve the Application by "npm run start"

Open http://localhost:1000/ in the browser to view the application
